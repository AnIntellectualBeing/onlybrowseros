# OnlyBrowserOS: compressed swap in RAM. Browser memory compresses to roughly
# a third, so a 1 GB laptop holds noticeably more before anything slows down.
[zram0]
zram-size = min(ram, 4096)
compression-algorithm = zstd
swap-priority = 100
