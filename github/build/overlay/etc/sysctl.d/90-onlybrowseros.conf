# OnlyBrowserOS memory tuning for compressed swap (zram).
# Swapping to RAM is cheap, so prefer it over dropping file cache, and read one
# page at a time instead of clusters (clusters only help spinning disks).
vm.swappiness = 150
vm.page-cluster = 0
vm.watermark_boost_factor = 0
vm.watermark_scale_factor = 125
